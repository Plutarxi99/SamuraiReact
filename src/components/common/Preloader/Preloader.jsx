import preloader from "../../../assets/image/blocks-shuffle-2.svg";

const Preloader = (props) => {
    return (
        <div className="preloader">
            <img src={preloader} alt="preloader"/>
        </div>
    )
}

export default Preloader;

